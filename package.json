{
  "name": "language-translator-app",
  "scripts": {
    "frontend-dev": "npm run dev --workspace=@translator/frontend",
    "deploy": "cd infrastructure; cdk deploy",
    "build-utils-lambda-layer": "npm run build --workspace @translator/utils-lambda-layer"
  },
  "workspaces": [
    "apps/*",
    "infrastructure",
    "packages/**"
  ]
}
